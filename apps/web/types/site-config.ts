// Re-exports for convenience: import types from "@/types/site-config"
// while values continue to come from "@/lib/site-config".

export type {
  AnimationConfig,
  AnimationPreset,
  Border,
  ColorOrGradient,
  ComponentNode,
  ComponentType,
  DataBinding,
  DetailDataSource,
  FooterConfig,
  FormDefinition,
  NavBarConfig,
  Page,
  PageKind,
  PaletteId,
  SafeParseSiteConfigResult,
  ShadowPreset,
  SiteConfig,
  SizeUnit,
  Spacing,
  StyleConfig,
} from "@/lib/site-config";
