"use client";

import type { SectionProps } from "./utils";

// Wave 2 stub. Sprint 11 (Presets + AI catalog + SPEC.md rewrite) replaces
// this with a 5-card preset gallery + confirmation dialog.
export function PresetPicker(_props: SectionProps) {
  return null;
}
