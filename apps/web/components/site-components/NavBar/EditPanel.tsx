// Sprint 8 fills in the Content / Style / Animation / Visibility / Advanced
// tabs for this component. For Sprint 5 we only ship the stub so the editor
// can locate the panel by data attribute.
export function NavBarEditPanel() {
  return <div data-component-edit-panel="NavBar" />;
}
